<!DOCTYPE html>
<html dir="{{ direction }}" lang="{{ lang }}">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <title>{{ title }}</title>
  <base href="{{ base }}" />
  {% if description %}
  <meta name="description" content="{{ description }}" />
  {% endif %}
  {% if keywords %}
  <meta name="keywords" content="{{ keywords }}" />
  {% endif %}
  <script src="{{ jquery }}" type="text/javascript"></script>
  <link href="{{ icons }}" rel="stylesheet" type="text/css" />
  <link href="{{ bootstrap }}" type="text/css" rel="stylesheet" />

  <link href="{{ stylesheet }}?v=4.9" type="text/css" rel="stylesheet" />
  <script src="catalog/view/javascript/common.js?v=1.1" type="text/javascript"></script>
  <script src="catalog/view/javascript/location.js?v=2" type="text/javascript"></script>

  {% if icon %}
  <link rel="icon" href="{{ icon }}" type="image/png">
  {% endif %}
  {% for style in styles %}
  <link href="{{ style.href }}" type="text/css" rel="{{ style.rel }}" media="{{ style.media }}" />
  {% endfor %}
  {% for script in scripts %}
  <script src="{{ script.href }}" type="text/javascript"></script>
  {% endfor %}
  {% for link in links %}
  <link href="{{ link.href }}" rel="{{ link.rel }}" />
  {% endfor %}
  {% for analytic in analytics %}
  {{ analytic }}
  {% endfor %}
</head>

<body>
  <!-- City Selection Popup (Loads on page load) -->
  <div class="modal fade" id="citySelectionModal" tabindex="-1" aria-labelledby="citySelectionModalLabel" aria-hidden="true" {% if user_city %}data-user-city-set="true"{% endif %}>
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content border-0 shadow-lg">
        <div class="modal-body p-5 text-center">
          <h3 class="fw-bold mb-3">Choose Your Location</h3>
          <p class="text-muted mb-4">Select your shipping country to view your local currency, delivery times, and costs.</p>
          
          <!-- City Dropdown -->
          <select class="form-select form-select-lg mb-4" id="citySelectionDropdown">
            {% for city in turkish_cities %}
            <option value="{{ city.name }}" {% if loop.first %}selected{% endif %}>{{ city.name }}</option>
            {% endfor %}
          </select>
          
          <!-- Confirm Button -->
          <button type="button" class="btn btn-dark btn-lg w-100 py-3" id="confirmCitySelection">
            Confirm Your Selection
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- Offcanvas Slider for Shopping Cart (Right Side) -->
  <div class="offcanvas offcanvas-end" tabindex="-1" id="cartModal" aria-labelledby="cartModalLabel">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title fw-bold" id="cartModalLabel">SHOPPING CART</h5>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body">
      <div id="cart-content-offcanvas">
        <div class="text-center py-5">
          <div class="spinner-border text-primary" role="status">
            <span class="visually-hidden">Loading...</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Offcanvas Slider for Location (Left Side) -->
  <div class="offcanvas offcanvas-start" tabindex="-1" id="locationModal" aria-labelledby="locationModalLabel">
    <div class="offcanvas-header pt-4">
      <div class="w-100 d-flex justify-content-center">
        <h5 class="offcanvas-title text-bold mb-0" id="locationModalLabel">CHANGE DELIVERY ADDRESS</h5>
      </div>
      <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body mx-auto w-50 d-flex flex-column" >
      <!-- 1. Address Input (Priority/Top) -->
      <div id="autocompleteInputGroup" class="mt-4 pt-4 mb-4 text-center">
        <label for="districtInput" class="form-label fw-bold">Enter your delivery address</label>
        <!-- Address input is now enabled by default -->
        <input type="text" class="form-control" id="districtInput" placeholder="Start typing your full address (e.g., Levent, Istanbul)...">
      </div>

      <!-- 2. Manual Selection Dropdowns -->
        <button type="button" onclick="$('#manualFieldsContainer').toggle();" class="btn btn-outline-none mb-3 w-100" id="toggleManualFieldsBtn">
          <i class="fa-solid fa-chevron-right"></i> ADD ADDRESS MANUALLY
        </button>
        <div id="manualFieldsContainer" style="display: none;">

        <!-- 2a. City Dropdown -->
        <div class="mb-3">
          <label for="cityDropdown" class="form-label">İl (Şehir)</label>
          <select class="form-select" id="cityDropdown">
            <option selected value="">Select a City...</option>
            {% for city in turkish_cities %}
            <option value="{{ city.name }}" {% if user_city == city.name %}selected{% endif %}>{{ city.name }}</option>
            {% endfor %}
          </select>
        </div>

        <!-- 2b. District Dropdown -->
        <div id="manualInputGroup" class="mb-3">
          <label for="manualDistrictDropdown" class="form-label">District (İlçe)</label>
          <select class="form-select" id="manualDistrictDropdown" disabled>
            <option selected value="">Select a District...</option>
          </select>
        </div>
      </div>

      <!-- Save Button -->
      <div class="mt-4">
        <button type="button" class="btn btn-primary w-100" id="saveLocationBtn" disabled>SET MY ADDRESS</button>
      </div>
    </div>
  </div>

  <div id="container">
    <header>
    <div id="alert"></div>
    <nav id="top">
      <div class="container-fluid">
        <div class="row">
          <div class="col-12 col-lg-4 pt-1 pl-2 px-lg-5 px-2" id="top-left">
                 <a id="openLocationModalBtn" {% if user_city and user_district %} data-location-set="true" {% endif %} href="#" class="dropdown-toggle" data-bs-toggle="offcanvas" data-bs-target="#locationModal" aria-label="Select location">
                    <i class="fa-solid fa-location-dot"></i>
                  {% if user_city and user_district %}
                    <span>
                    {{ user_district }}
                    </span>
                  {% else %}
                    <span>Your Location</span>
                  {% endif %}
                  </a>
                  <br class="d-none d-lg-block"/>
                <div class="dropdown d-none d-lg-inline-block">
                  
                    <a href="#" class="dropdown-toggle" data-bs-toggle="dropdown" aria-label="Select shipping city">
                     <span>Ships to {% if user_city %}{{ user_city }}{% else %}Select City{% endif %}</span><i class="fa-solid fa-caret-down"></i>
                    </a>
                  <ul class="dropdown-menu">
                    {% for city in turkish_cities %}
                    <li><a href="#" class="dropdown-item ship-to-city" data-city="{{ city.name }}" onclick="event.preventDefault(); setShipToCity('{{ city.name }}');">{{ city.name }}</a></li>
                    {% endfor %}
                  </ul>
                 
                </div>

          </div>
          <div class="col-4 d-lg-none" id="logo-left-mobile">
                <a href="#" id="toggle-search" aria-label="Toggle search" class="px-3"><i class="fa-solid fa-search"></i></a>

            <button type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileMenuModal" class="navbar-toggler"><i class="fa-solid fa-bars"></i></button>
          </div>
          <div class="col-4 col-lg-4 d-flex justify-content-center align-items-center">
            <a href="{{ home }}" class="d-inline-block text-center">
              <img src="{{ logo }}" title="{{ name }}" alt="{{ name }}" class="img-fluid" />
            </a>
          </div>

          <div class="col-4 col-lg-3 text-end" id="logo-right-mobile">
            <ul class="list-inline">
              <li class="list-inline-item d-none d-lg-inline-block">
                <a href="#" id="toggle-search" aria-label="Toggle search"><i class="fa-solid fa-search"></i></a>
              </li>



              <li class="list-inline-item">
                <div class="dropdown">
                  <a href="#" class="dropdown-toggle" data-bs-toggle="dropdown"><i class="fa-regular fa-user"></i> <span class="d-none d-lg-inline"></span> </a>
                  <ul class="dropdown-menu dropdown-menu-right">
                    {% if not logged %}
                    <li><a href="{{ register }}" class="dropdown-item">{{ text_register }}</a></li>
                    <li><a href="{{ login }}" class="dropdown-item">{{ text_login }}</a></li>
                    {% else %}
                    <li><a href="{{ account }}" class="dropdown-item">{{ text_account }}</a></li>
                    <li><a href="{{ order }}" class="dropdown-item">{{ text_order }}</a></li>
                    <li><a href="{{ transaction }}" class="dropdown-item">{{ text_transaction }}</a></li>
                    <li><a href="{{ download }}" class="dropdown-item">{{ text_download }}</a></li>
                    <li><a href="{{ logout }}" class="dropdown-item">{{ text_logout }}</a></li>
                    {% endif %}
                  </ul>
                </div>
              </li>
              <li class="list-inline-item  d-none d-lg-inline-block"><a href="{{ wishlist }}" id="wishlist-total" title="{{ text_wishlist }}"><i class="fa-regular fa-heart"></i> <span class="d-none d-lg-inline"></span></a></li>
              <li class="list-inline-item"><a href="#" id="cart-toggle" data-bs-toggle="offcanvas" data-bs-target="#cartModal" title="{{ text_shopping_cart }}"><i class="fa-solid fa-cart-shopping"></i> <span class="d-none d-lg-inline"></span></a></li>
            </ul>
          </div>
          
        </div>
      </div>
    </nav>
    {{ menu }}
      <div class="container">
        <div class="row">
          <div class="col">
            {{ search }}
          </div>
        </div>
        
    </header>
   
      <script>
        // CRITICAL: Replace THIS_IS_YOUR_GOOGLE_MAPS_API_KEY with your actual API key
        // Ensure the Google Maps API key has the 'Places API' enabled.
        const GOOGLE_API_KEY = "{{ google_api_key }}";
        // JSON list of cities and common districts for manual selection
        const turkishCitiesAndDistricts = {{ turkish_cities_and_districts_json|raw }};

       
      </script>

    <main class="flex-grow-1 mb-4 pt-4">
